# Requirements for Education Creator with Vector Database
# Updated with HuggingFace Inference Client

# Google Gemini API for text generation and embeddings
google-generativeai>=0.3.0

# HuggingFace for image generation (fal-ai provider)
huggingface-hub>=0.20.0

# Vector operations
numpy>=1.24.0

# Image processing
pillow>=10.0.0

# Environment variables
python-dotenv>=1.0.0

# HTTP requests (if needed)
requests>=2.31.0